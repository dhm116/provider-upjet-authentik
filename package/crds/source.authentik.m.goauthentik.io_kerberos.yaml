---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: kerberos.source.authentik.m.goauthentik.io
spec:
  group: source.authentik.m.goauthentik.io
  names:
    categories:
    - crossplane
    - managed
    - authentik
    kind: Kerberos
    listKind: KerberosList
    plural: kerberos
    singular: kerberos
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Kerberos is the Schema for the Kerbeross API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KerberosSpec defines the desired state of Kerberos
            properties:
              forProvider:
                properties:
                  authenticationFlow:
                    type: string
                  enabled:
                    description: Defaults to `true`.
                    type: boolean
                  enrollmentFlow:
                    type: string
                  groupMatchingMode:
                    description: |-
                      Allowed values:
                      - `identifier`
                      - `name_link`
                      - `name_deny`
                      Defaults to `identifier`.
                    type: string
                  krb5Conf:
                    description: Custom krb5.conf to use. Uses the system one by default
                    type: string
                  name:
                    type: string
                  passwordLoginUpdateInternalPassword:
                    description: If enabled, the authentik-stored password will be
                      updated upon login with the Kerberos password backend Defaults
                      to `false`.
                    type: boolean
                  policyEngineMode:
                    description: |-
                      Allowed values:
                      - `all`
                      - `any`
                      Defaults to `any`.
                    type: string
                  realm:
                    description: Kerberos realm
                    type: string
                  slug:
                    type: string
                  spnegoCcache:
                    description: Credential cache to use for SPNEGO in form type:residual
                    type: string
                  spnegoKeytabSecretRef:
                    description: SPNEGO keytab base64-encoded or path to keytab in
                      the form FILE:path
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  spnegoServerName:
                    description: Force the use of a specific server name for SPNEGO
                    type: string
                  syncCcache:
                    description: Credentials cache to authenticate to kadmin for sync.
                      Must be in the form TYPE:residual
                    type: string
                  syncKeytabSecretRef:
                    description: Keytab to authenticate to kadmin for sync. Must be
                      base64-encoded or in the form TYPE:residual
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  syncOutgoingTriggerMode:
                    description: |-
                      Allowed values:
                      - `none`
                      - `immediate`
                      - `deferred_end`
                      Defaults to `deferred_end`.
                    type: string
                  syncPasswordSecretRef:
                    description: Password to authenticate to kadmin for sync
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  syncPrincipal:
                    description: Principal to authenticate to kadmin for sync.
                    type: string
                  syncUsers:
                    description: Sync users from Kerberos into authentik Defaults
                      to `true`.
                    type: boolean
                  syncUsersPassword:
                    description: When a user changes their password, sync it back
                      to Kerberos Defaults to `true`.
                    type: boolean
                  userMatchingMode:
                    description: |-
                      Allowed values:
                      - `identifier`
                      - `email_link`
                      - `email_deny`
                      - `username_link`
                      - `username_deny`
                      Defaults to `identifier`.
                    type: string
                  userPathTemplate:
                    description: Defaults to `goauthentik.io/sources/%(slug)s`.
                    type: string
                  uuid:
                    description: Generated.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authenticationFlow:
                    type: string
                  enabled:
                    description: Defaults to `true`.
                    type: boolean
                  enrollmentFlow:
                    type: string
                  groupMatchingMode:
                    description: |-
                      Allowed values:
                      - `identifier`
                      - `name_link`
                      - `name_deny`
                      Defaults to `identifier`.
                    type: string
                  krb5Conf:
                    description: Custom krb5.conf to use. Uses the system one by default
                    type: string
                  name:
                    type: string
                  passwordLoginUpdateInternalPassword:
                    description: If enabled, the authentik-stored password will be
                      updated upon login with the Kerberos password backend Defaults
                      to `false`.
                    type: boolean
                  policyEngineMode:
                    description: |-
                      Allowed values:
                      - `all`
                      - `any`
                      Defaults to `any`.
                    type: string
                  realm:
                    description: Kerberos realm
                    type: string
                  slug:
                    type: string
                  spnegoCcache:
                    description: Credential cache to use for SPNEGO in form type:residual
                    type: string
                  spnegoKeytabSecretRef:
                    description: SPNEGO keytab base64-encoded or path to keytab in
                      the form FILE:path
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  spnegoServerName:
                    description: Force the use of a specific server name for SPNEGO
                    type: string
                  syncCcache:
                    description: Credentials cache to authenticate to kadmin for sync.
                      Must be in the form TYPE:residual
                    type: string
                  syncKeytabSecretRef:
                    description: Keytab to authenticate to kadmin for sync. Must be
                      base64-encoded or in the form TYPE:residual
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  syncOutgoingTriggerMode:
                    description: |-
                      Allowed values:
                      - `none`
                      - `immediate`
                      - `deferred_end`
                      Defaults to `deferred_end`.
                    type: string
                  syncPasswordSecretRef:
                    description: Password to authenticate to kadmin for sync
                    properties:
                      key:
                        type: string
                      name:
                        description: Name of the secret.
                        type: string
                    required:
                    - key
                    - name
                    type: object
                  syncPrincipal:
                    description: Principal to authenticate to kadmin for sync.
                    type: string
                  syncUsers:
                    description: Sync users from Kerberos into authentik Defaults
                      to `true`.
                    type: boolean
                  syncUsersPassword:
                    description: When a user changes their password, sync it back
                      to Kerberos Defaults to `true`.
                    type: boolean
                  userMatchingMode:
                    description: |-
                      Allowed values:
                      - `identifier`
                      - `email_link`
                      - `email_deny`
                      - `username_link`
                      - `username_deny`
                      Defaults to `identifier`.
                    type: string
                  userPathTemplate:
                    description: Defaults to `goauthentik.io/sources/%(slug)s`.
                    type: string
                  uuid:
                    description: Generated.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
            - message: spec.forProvider.realm is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.realm)
                || (has(self.initProvider) && has(self.initProvider.realm))'
            - message: spec.forProvider.slug is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.slug)
                || (has(self.initProvider) && has(self.initProvider.slug))'
          status:
            description: KerberosStatus defines the observed state of Kerberos.
            properties:
              atProvider:
                properties:
                  authenticationFlow:
                    type: string
                  enabled:
                    description: Defaults to `true`.
                    type: boolean
                  enrollmentFlow:
                    type: string
                  groupMatchingMode:
                    description: |-
                      Allowed values:
                      - `identifier`
                      - `name_link`
                      - `name_deny`
                      Defaults to `identifier`.
                    type: string
                  id:
                    type: string
                  krb5Conf:
                    description: Custom krb5.conf to use. Uses the system one by default
                    type: string
                  name:
                    type: string
                  passwordLoginUpdateInternalPassword:
                    description: If enabled, the authentik-stored password will be
                      updated upon login with the Kerberos password backend Defaults
                      to `false`.
                    type: boolean
                  policyEngineMode:
                    description: |-
                      Allowed values:
                      - `all`
                      - `any`
                      Defaults to `any`.
                    type: string
                  realm:
                    description: Kerberos realm
                    type: string
                  slug:
                    type: string
                  spnegoCcache:
                    description: Credential cache to use for SPNEGO in form type:residual
                    type: string
                  spnegoServerName:
                    description: Force the use of a specific server name for SPNEGO
                    type: string
                  syncCcache:
                    description: Credentials cache to authenticate to kadmin for sync.
                      Must be in the form TYPE:residual
                    type: string
                  syncOutgoingTriggerMode:
                    description: |-
                      Allowed values:
                      - `none`
                      - `immediate`
                      - `deferred_end`
                      Defaults to `deferred_end`.
                    type: string
                  syncPrincipal:
                    description: Principal to authenticate to kadmin for sync.
                    type: string
                  syncUsers:
                    description: Sync users from Kerberos into authentik Defaults
                      to `true`.
                    type: boolean
                  syncUsersPassword:
                    description: When a user changes their password, sync it back
                      to Kerberos Defaults to `true`.
                    type: boolean
                  userMatchingMode:
                    description: |-
                      Allowed values:
                      - `identifier`
                      - `email_link`
                      - `email_deny`
                      - `username_link`
                      - `username_deny`
                      Defaults to `identifier`.
                    type: string
                  userPathTemplate:
                    description: Defaults to `goauthentik.io/sources/%(slug)s`.
                    type: string
                  uuid:
                    description: Generated.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
